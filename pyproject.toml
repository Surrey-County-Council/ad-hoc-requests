[project]
name = "ad-hoc-requests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"

requires-python = ">=3.13"
dependencies = []

[project.scripts]
ad-hoc-requests = "ad_hoc_requests:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"
